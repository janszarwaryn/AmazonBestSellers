# Database Configuration
# Generate secure password: openssl rand -base64 32 | tr -d '\n'
DB_ROOT_PASSWORD=generate_secure_password_min_32_chars
DB_HOST=localhost
DB_PORT=3306
DB_NAME=AmazonBestSellersDb
DB_USER=your_db_username
DB_PASSWORD=generate_secure_password_min_32_chars

# JWT Configuration
# Generate secure secret: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=generate_jwt_secret_minimum_64_characters_base64_encoded
JWT_ISSUER=AmazonBestSellersAPI
JWT_AUDIENCE=AmazonBestSellersClient
JWT_EXPIRE_MINUTES=60

# RapidAPI Configuration
# Get your API key from: https://rapidapi.com/letscrape-6bRBa3QguO5/api/real-time-amazon-data
RAPIDAPI_KEY=your_rapidapi_key_from_dashboard
RAPIDAPI_HOST=real-time-amazon-data.p.rapidapi.com
RAPIDAPI_BASE_URL=https://real-time-amazon-data.p.rapidapi.com

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Admin Credentials (CHANGE THESE IMMEDIATELY!)
# Use strong password with min 16 chars, uppercase, lowercase, digits, and symbols
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=generate_secure_password_min_16_chars_with_symbols

# Logging
LOG_PATH=logs/log-.txt
